name: GitHub Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  # Tags are not covered by the branch and pull request triggers on CI, so
  # nothing else re-checks the tagged tree. Collecting coverage runs the whole
  # workspace test suite, so this gates the release on the tests passing and on
  # coverage baseline holding. Everything downstream of the release workflow
  # inherits the gate.
  coverage:
    name: Coverage gate
    uses: ./.github/workflows/coverage.yml

  build-linux:
    name: Build x86_64-unknown-linux-gnu
    needs: coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.97.1
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Install Linux desktop and audio dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends --assume-yes libasound2-dev libwebkit2gtk-4.1-dev

      - name: Build release binary
        run: cargo build --release --locked --target x86_64-unknown-linux-gnu

      - name: Build voice worker
        run: cargo build --release --locked -p belgr-mj-voice-worker --target x86_64-unknown-linux-gnu

      - name: Package archive
        shell: bash
        run: |
          name="belgr-${GITHUB_REF_NAME}-x86_64-unknown-linux-gnu"
          mkdir "$name"
          cp "target/x86_64-unknown-linux-gnu/release/belgr" "$name/"
          cp "target/x86_64-unknown-linux-gnu/release/belgr-voice-worker" "$name/"
          cp README.md LICENSE "$name/"
          mkdir "$name/licenses"
          cp licenses/SOURCE.md licenses/OFL-1.1.md licenses/THIRD_PARTY_LICENSES.html licenses/SUPPLEMENTAL_THIRD_PARTY_NOTICES.txt "$name/licenses/"
          cp -R licenses/native "$name/licenses/"
          tar czf "$name.tar.gz" "$name"
          shasum -a 256 "$name.tar.gz" > "$name.tar.gz.sha256"

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: x86_64-unknown-linux-gnu
          path: |
            belgr-*.tar.gz
            belgr-*.tar.gz.sha256
          if-no-files-found: error

  build-linux-arm64:
    name: Build aarch64-unknown-linux-gnu
    needs: coverage
    # Build natively on a GitHub-hosted arm64 runner instead of cross. The
    # cross images (ghcr.io/cross-rs/aarch64-unknown-linux-gnu, Ubuntu 20.04 /
    # GCC 9 even on the main tag) ship a libstdc++ too old to link the
    # GCC 11+ symbols in the prebuilt sherpa-onnx aarch64 static libs
    # (std::__throw_bad_array_new_length, std::__cxx11::basic_stringstream,
    # std::filesystem::path). ubuntu-24.04-arm ships GCC 13, matching the
    # native build path that already succeeds on x86_64.
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.97.1
        with:
          targets: aarch64-unknown-linux-gnu

      - name: Install Linux desktop and audio dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends --assume-yes libasound2-dev libwebkit2gtk-4.1-dev

      - name: Build release binary
        run: cargo build --release --locked --target aarch64-unknown-linux-gnu

      - name: Build voice worker
        run: cargo build --release --locked -p belgr-mj-voice-worker --target aarch64-unknown-linux-gnu

      - name: Package archive
        shell: bash
        run: |
          name="belgr-${GITHUB_REF_NAME}-aarch64-unknown-linux-gnu"
          mkdir "$name"
          cp "target/aarch64-unknown-linux-gnu/release/belgr" "$name/"
          cp "target/aarch64-unknown-linux-gnu/release/belgr-voice-worker" "$name/"
          cp README.md LICENSE "$name/"
          mkdir "$name/licenses"
          cp licenses/SOURCE.md licenses/OFL-1.1.md licenses/THIRD_PARTY_LICENSES.html licenses/SUPPLEMENTAL_THIRD_PARTY_NOTICES.txt "$name/licenses/"
          cp -R licenses/native "$name/licenses/"
          tar czf "$name.tar.gz" "$name"
          shasum -a 256 "$name.tar.gz" > "$name.tar.gz.sha256"

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: aarch64-unknown-linux-gnu
          path: |
            belgr-*.tar.gz
            belgr-*.tar.gz.sha256
          if-no-files-found: error

  build-android:
    name: Build aarch64-linux-android
    needs: coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.97.1
        with:
          targets: aarch64-linux-android

      - name: Setup Android NDK
        id: setup-ndk
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r27d

      - name: Export Android NDK toolchain
        shell: bash
        run: |
          ndk_path='${{ steps.setup-ndk.outputs.ndk-path }}'
          echo "ANDROID_NDK_HOME=$ndk_path" >> "$GITHUB_ENV"
          echo "ANDROID_NDK_ROOT=$ndk_path" >> "$GITHUB_ENV"
          echo "CARGO_TARGET_AARCH64_LINUX_ANDROID_LINKER=$ndk_path/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android21-clang" >> "$GITHUB_ENV"
          echo "CC_aarch64_linux_android=$ndk_path/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android21-clang" >> "$GITHUB_ENV"
          echo "AR_aarch64_linux_android=$ndk_path/toolchains/llvm/prebuilt/linux-x86_64/bin/llvm-ar" >> "$GITHUB_ENV"

      - name: Build release binary
        run: cargo build --release --locked --no-default-features --target aarch64-linux-android

      - name: Package archive
        shell: bash
        run: |
          name="belgr-${GITHUB_REF_NAME}-aarch64-linux-android"
          mkdir "$name"
          cp "target/aarch64-linux-android/release/belgr" "$name/"
          cp README.md LICENSE "$name/"
          mkdir "$name/licenses"
          cp licenses/SOURCE.md licenses/OFL-1.1.md licenses/THIRD_PARTY_LICENSES.html licenses/SUPPLEMENTAL_THIRD_PARTY_NOTICES.txt "$name/licenses/"
          cp -R licenses/native "$name/licenses/"
          tar czf "$name.tar.gz" "$name"
          shasum -a 256 "$name.tar.gz" > "$name.tar.gz.sha256"

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: aarch64-linux-android
          path: |
            belgr-*.tar.gz
            belgr-*.tar.gz.sha256
          if-no-files-found: error

  build-windows:
    name: Build x86_64-pc-windows-msvc
    needs: coverage
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.97.1
        with:
          targets: x86_64-pc-windows-msvc

      - name: Build release binary
        run: cargo build --release --locked --target x86_64-pc-windows-msvc

      - name: Build voice worker
        run: cargo build --release --locked -p belgr-mj-voice-worker --target x86_64-pc-windows-msvc

      - name: Package archive
        shell: pwsh
        run: |
          $name = "belgr-${env:GITHUB_REF_NAME}-x86_64-pc-windows-msvc"
          New-Item -ItemType Directory -Path $name | Out-Null
          Copy-Item "target\x86_64-pc-windows-msvc\release\belgr.exe" -Destination $name
          Copy-Item "target\x86_64-pc-windows-msvc\release\belgr-voice-worker.exe" -Destination $name
          Copy-Item "README.md" -Destination $name
          Copy-Item "LICENSE" -Destination $name
          New-Item -ItemType Directory -Path "$name\licenses" | Out-Null
          $legalFiles = @("licenses\SOURCE.md", "licenses\OFL-1.1.md", "licenses\THIRD_PARTY_LICENSES.html", "licenses\SUPPLEMENTAL_THIRD_PARTY_NOTICES.txt")
          Copy-Item $legalFiles -Destination "$name\licenses"
          Copy-Item "licenses\native" -Destination "$name\licenses" -Recurse
          Compress-Archive -Path $name -DestinationPath "$name.zip"
          $hash = (Get-FileHash -Algorithm SHA256 "$name.zip").Hash.ToLower()
          "$hash  $name.zip" | Set-Content -Path "$name.zip.sha256" -NoNewline

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: x86_64-pc-windows-msvc
          path: |
            belgr-*.zip
            belgr-*.zip.sha256
          if-no-files-found: error

  build-macos:
    name: Build macOS universal
    needs: coverage
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.97.1
        with:
          targets: x86_64-apple-darwin,aarch64-apple-darwin

      - name: Build x86_64 binary
        run: cargo build --release --locked --target x86_64-apple-darwin

      - name: Build x86_64 voice worker
        run: cargo build --release --locked -p belgr-mj-voice-worker --target x86_64-apple-darwin

      - name: Build aarch64 binary
        run: cargo build --release --locked --target aarch64-apple-darwin

      - name: Build aarch64 voice worker
        run: cargo build --release --locked -p belgr-mj-voice-worker --target aarch64-apple-darwin

      - name: Create universal binary
        run: |
          mkdir -p target/universal-apple-darwin/release
          lipo -create \
            target/x86_64-apple-darwin/release/belgr \
            target/aarch64-apple-darwin/release/belgr \
            -output target/universal-apple-darwin/release/belgr
          lipo -info target/universal-apple-darwin/release/belgr
          lipo -create \
            target/x86_64-apple-darwin/release/belgr-voice-worker \
            target/aarch64-apple-darwin/release/belgr-voice-worker \
            -output target/universal-apple-darwin/release/belgr-voice-worker
          lipo -info target/universal-apple-darwin/release/belgr-voice-worker

      - name: Package archive
        run: |
          name="belgr-${GITHUB_REF_NAME}-universal-apple-darwin"
          mkdir "$name"
          cp target/universal-apple-darwin/release/belgr "$name/"
          cp target/universal-apple-darwin/release/belgr-voice-worker "$name/"
          cp README.md LICENSE "$name/"
          mkdir "$name/licenses"
          cp licenses/SOURCE.md licenses/OFL-1.1.md licenses/THIRD_PARTY_LICENSES.html licenses/SUPPLEMENTAL_THIRD_PARTY_NOTICES.txt "$name/licenses/"
          cp -R licenses/native "$name/licenses/"
          tar czf "$name.tar.gz" "$name"
          shasum -a 256 "$name.tar.gz" > "$name.tar.gz.sha256"

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: universal-apple-darwin
          path: |
            belgr-*.tar.gz
            belgr-*.tar.gz.sha256
          if-no-files-found: error

  release:
    name: Create GitHub Release
    needs: [build-linux, build-linux-arm64, build-android, build-windows, build-macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Create release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          files: |
            artifacts/belgr-*.tar.gz
            artifacts/belgr-*.tar.gz.sha256
            artifacts/belgr-*.zip
            artifacts/belgr-*.zip.sha256

      - name: Announce release on Discord
        continue-on-error: true
        env:
          DISCORD_RELEASE_WEBHOOK_URL: ${{ secrets.DISCORD_RELEASE_WEBHOOK_URL }}
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.ref_name }}
        shell: bash
        run: bash scripts/announce-discord-release.sh
