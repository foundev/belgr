---
title: Mjolnir Web
description: One command publishes the coding session on your machine to a browser — read the diff, steer a running turn, answer permission prompts, and start worktree sessions from your phone while the repository stays on the host.
---

import { Image } from 'astro:assets';
import shotLogin from '../../assets/remote/login.png';
import shotNewSession from '../../assets/remote/new-session-worktree.png';
import shotFolderSearch from '../../assets/remote/folder-search.png';
import shotMobileSessions from '../../assets/remote/mobile-sessions.png';
import shotSubagentApproval from '../../assets/remote/subagent-approval.png';
import shotCommandPalette from '../../assets/remote/command-palette.png';
import shotSideConversation from '../../assets/remote/side-conversation.png';
import shotSidePalette from '../../assets/remote/side-palette.png';
import shotSessionConfig from '../../assets/remote/session-config.png';
import shotStatusLine from '../../assets/remote/status-line.png';
import shotHistory from '../../assets/remote/history-archive.png';

One command turns the coding session on your machine into a browser you can
drive from the next room — or, over Tailscale, from anywhere:

```bash
mj server
```

Mjolnir Web is the same session, not a summary of it. It runs with the resolved
team, models, permissions, and subagent configuration the terminal client uses,
and it renders what the TUI renders: streaming turns, file diffs, terminal
output, permission prompts, per-seat token usage, and the independent review
pass. The repository, the agent processes, and the transcripts stay on the host.
The browser is a control surface for that session, not a copy of your code.

<figure class="shot">
  <a class="shot__zoom" href={shotHistory.src} aria-label="Open the full-size screenshot: the full session view">
    <Image src={shotHistory} alt="Mjolnir Web on a desktop browser: active and archived sessions in the sidebar, a completed agent turn with file links, per-seat token usage, and its review result." />
  </a>
  <figcaption>A real completed session — the agent result, its review pass, per-seat token totals, and active and archived work in the sidebar.</figcaption>
</figure>

## What you can do from a browser

- **Answer permission prompts** with the diff or command in front of you — deny,
  allow once, or always allow.
- **Read the work as it happens**: streaming assistant turns, tool calls, file
  diffs, and terminal output.
- **Start new work**, including in a fresh Git worktree, without touching the
  terminal.
- **Pick up sessions from either side.** Sessions started in the terminal appear
  in the browser; sessions started in the browser record their adapter and model
  so `mj resume` reopens them on the host.
- **Steer the running turn** — send a correction while the agent is working
  to place it in the same FIFO queue the terminal uses, then select **Stop**
  to inject the oldest queued correction into that turn when steering is
  supported.
- **Attach an image** to a prompt, and run the agent's own slash commands: typing
  `/` opens a palette of the commands the connected agent advertises.
- **Install it as an app.** Mjolnir Web ships a web app manifest and service
  worker, so a phone can add it to the home screen and open it standalone.

## Start the server

```bash
mj server
```

Without an explicit hostname or a usable Tailscale node, the server listens on
loopback HTTPS (`127.0.0.1` and `::1`) on port 11921 with a locally generated
certificate. It is then reachable only from the same machine, so it prints no
device-login QR code. On a certificate-capable Tailscale node, detection happens
automatically and the server is reachable over the tailnet; `--hostname` exposes
the server under an explicit name instead. On start, `mj server` prints the URL
to open, the six-digit viewer code, and the viewer session lifetime.

## Open the native desktop app

```bash
mj app
```

`mj app` opens the same viewer in a native window and starts its own loopback
listener. The first app tries port 11922; further app instances take the next
available port. Each listener registers in the shared session database and
heartbeats once per minute. Terminal sessions query that registry as part of
their normal remote-control requests: they use a live `mj server` first, then
the most recently heartbeating app. If an endpoint has died but its row has not
expired yet, they try the next live candidate; rows from crashed processes age
out after two minutes.

The app is deliberately local-only. It does not replace `mj server`, so a
server with Tailscale or an explicit hostname remains the primary browser and
remote-device entry point. All local endpoints use the same session database,
bearer token, and stable loopback TLS identity, so starting or stopping either
one does not fork session state.

## Choose a port

```bash
mj server --port 9443
```

`--port` moves every listener, the printed viewer URL, and the login QR code to
that port. It applies to automatic Tailscale and explicit `--hostname` mode as
well. A live server remains the local terminal clients' first choice regardless
of its port; they otherwise use a live `mj app` listener from the shared
registry.

Sign in with that viewer code or a bearer login token; the browser then stores a
signed session cookie. Treat QR codes, login URLs, tokens, cookies, certificate
keys, and downloaded transcripts as secrets.

<figure class="shot">
  <a class="shot__zoom" href={shotLogin.src} aria-label="Open the full-size screenshot of the sign-in screen">
    <Image src={shotLogin} alt="The Mjolnir Web sign-in screen: a panel labelled REMOTE CONSOLE asking for the six-digit viewer code printed by mj server." />
  </a>
  <figcaption>Sign-in takes the six-digit code <code>mj server</code> prints, or the login URL behind the QR code when the server is reachable off the host.</figcaption>
</figure>

## From your phone

The phone layout is the whole session, not a cut-down status page. Approve a
write against the diff that produced it, watch the reviewer and subagents work,
and send the next prompt. Add it to the home screen and it opens standalone,
like any other app. A phone reaches the server once it is off loopback — see
[Reach it from another device](#reach-it-from-another-device).

<figure class="shot shot--phone">
  <a class="shot__zoom" href={shotMobileSessions.src} aria-label="Open the full-size screenshot: the session list on a phone">
    <Image src={shotMobileSessions} alt="Mjolnir Web on a phone showing the session list: two weather-cli sessions with their worktree names, models, message counts, and relative times, above a History row." />
  </a>
  <figcaption>The session list on a phone — every session on the host, each tagged with its worktree and model.</figcaption>
</figure>

## Start new work from anywhere

The new-session dialog is one screen: current folder, a folder search, a **New
worktree** checkbox, and **Start session**.

<figure class="shot">
  <a class="shot__zoom" href={shotNewSession.src} aria-label="Open the full-size screenshot: the new-session dialog">
    <Image src={shotNewSession} alt="The NEW SESSION dialog: current folder /Users/Shared/mj-demo, a recent-folders row, a folder search box, a checked New worktree option, a Start session button, and the three project folders below." />
  </a>
  <figcaption>Pick a folder, tick <strong>New worktree</strong>, start. Recent working directories sit above the folder list so yesterday’s project is one tap away.</figcaption>
</figure>

The folder picker browses the workspace roots configured on the server. It also
shows up to six working directories from the most recently used retained
sessions and can search for nested folders across those roots. Recent and search
results are canonicalized against the current roots before they are shown, so
changing the server's workspace configuration does not expose old paths. Broad
searches are capped; refine the query when the picker reports that only the
first matches are shown.

<figure class="shot">
  <a class="shot__zoom" href={shotFolderSearch.src} aria-label="Open the full-size screenshot: the folder search">
    <Image src={shotFolderSearch} alt="The new-session folder search showing one matching folder, /Users/Shared/mj-demo/invoice-api/src/handlers, with the match count below it." />
  </a>
  <figcaption>Search reaches nested folders across every configured root, and says how many matches it is showing.</figcaption>
</figure>

## Permissions with the change in view

Every permission request the session raises reaches the browser with the diff or
command that prompted it, so an approval from a phone is still an informed one.

<figure class="shot">
  <a class="shot__zoom" href={shotSubagentApproval.src} aria-label="Open the full-size screenshot: the MCP subagent approval">
    <Image src={shotSubagentApproval} alt="Mjolnir Web showing an INPUT NEEDED card asking whether the mj-subagents MCP server may run the create_subagent tool, with Allow once, Allow for this session, Allow and don’t ask again, Cancel, and Decline." />
  </a>
  <figcaption>An MCP approval for Mjolnir’s own subagent tool, with every approval and rejection choice the agent offers.</figcaption>
</figure>

Requests raised by a subagent carry its id, so a remote user answers the right
nested request while several run at once. MCP approvals for
`mcp__mj_subagents__create_subagent` and `mcp__mj_subagents__subagent_cancel`
are recognized as Mjolnir's own tools rather than third-party ones.

## Subagents in the browser

Mjolnir Web keeps an actor-level subagent list: one row per background subagent,
running rows first in spawn order, each showing its id, label, latest activity,
and elapsed time, and finished rows marked `✔`, `✘`, or `⊘`. This is more
granular than the terminal's aggregate workflow progress rows.

## Side conversations

A side conversation is a throwaway fork of the primary session: ask a question,
think out loud, or get a second opinion without the exchange landing in the
main transcript or steering the agent's real work. Typing `/side` (with an
optional question) in the web composer forks the session; everything that
follows is labelled with the `Side` actor, the composer switches to side mode,
and **Exit side** (or sending `exit`) deletes the fork and returns you to the
main conversation.

<figure class="shot">
  <a class="shot__zoom" href={shotSideConversation.src} aria-label="Open the full-size screenshot: a side conversation in Mjolnir Web">
    <Image src={shotSideConversation} alt="Mjolnir Web on a Claude coder + Codex reviewer session. Below a blue PRIMARY usage entry, amber SIDE entries read Side conversation started, session forked, a user question asking whether a dict of city defaults would be cleaner than hardcoding values, and the agent's reply with a CITY_DEFAULTS code block. The header shows a SIDE badge and the composer reads Message the side conversation — type exit to return, with an Exit side button." />
  </a>
  <figcaption>A side conversation on a Claude coder + Codex reviewer session: the main turn's <code>PRIMARY</code> entry above, the forked <code>SIDE</code> exchange below, and the composer in side mode with <strong>Exit side</strong>.</figcaption>
</figure>

Side conversations are available only on **Claude-primary teams**. They belong
to the primary session, not the reviewer, so the team's coder decides whether
`/side` exists at all:

| Team | `/side` |
| --- | --- |
| Claude | Yes |
| Claude coder + Codex reviewer | Yes |
| Codex | No |
| Codex coder + Claude reviewer | No |

Mjolnir only exposes `/side` when the primary agent advertises ACP
`session/fork`, `session/resume` (or `session/load`), and `session/delete`.
Claude's ACP adapter advertises all three; Codex's advertises load/resume and
delete but not `session/fork`, so Codex-primary sessions never see the command.

Typing `/` opens the palette of commands the connected agent advertises, so the
list reflects that agent's capabilities rather than a fixed menu. On a
Claude-primary session, filtering the palette brings up `/side`:

<figure class="shot">
  <a class="shot__zoom" href={shotSidePalette.src} aria-label="Open the full-size screenshot: the command palette filtered to /side">
    <Image src={shotSidePalette} alt="The Mjolnir Web composer with /si typed on a Claude session, the palette filtered to two entries: /simplify and /side <optional question> — open an isolated ephemeral conversation." />
  </a>
  <figcaption>The palette on a Claude-primary session filtered to <code>/si</code>: <code>/side &lt;optional question&gt;</code> opens an isolated ephemeral conversation.</figcaption>
</figure>

For comparison, the same palette on a Codex-primary session lists the commands
that agent advertises and has no `/side` entry:

<figure class="shot">
  <a class="shot__zoom" href={shotCommandPalette.src} aria-label="Open the full-size screenshot: the command palette on a Codex-primary session">
    <Image src={shotCommandPalette} alt="The Mjolnir Web composer with a slash typed on a Codex session, showing a command palette listing /new, /clear, /compact, /export, /mjconfig, and discrete-review commands with their descriptions. /side is not listed." />
  </a>
  <figcaption>The same palette on a Codex-primary session — no <code>/side</code>, because codex-acp does not advertise <code>session/fork</code>.</figcaption>
</figure>

## Status line

Under the chat header the browser mirrors the terminal status line: primary
model and its ACP adapter, reasoning effort, per-seat token totals
(primary/review/subagents), context occupancy when the agent reports it,
provider subscription quota windows (Codex/Claude), and a link to the current
branch's open pull request. The pull-request badge comes from a `git`/`gh` probe
on the session host and refreshes about twice a minute.

<figure class="shot">
  <a class="shot__zoom" href={shotStatusLine.src} aria-label="Open the full-size screenshot: the status line">
    <Image src={shotStatusLine} alt="The Mjolnir Web status line: gpt-5-6-sol via codex-acp, effort default, primary 17.7k, review 18k, context 7 percent, and a Codex usage line showing remaining weekly quota and reset time." />
  </a>
  <figcaption>The status strip under the chat header: model and adapter, effort, per-seat totals, context occupancy, and the provider quota window.</figcaption>
</figure>

Cost is deliberately not shown here. Only some ACP adapters report one, and seats
bind adapters independently, so a single session-wide figure would silently cover
just the seats that happen to report. `/agents` in the terminal breaks cost down
per seat and per model, where the unpriced seats are visible.

## Archiving sessions

The browser can archive a server-owned session to move it out of the active list
and load or unarchive it later. A live TUI session cannot be archived; exit it in
the terminal first. Archiving changes where the session is listed, not its
retained transcript data.

## Configuration from the browser

`/mjconfig` opens the same configuration editor the terminal uses: team,
reviewer, subagent, and ACP-server settings, plus the session options the
connected adapter exposes. Changes apply to sessions started afterwards, not to
the turn already running. The primary session's model and reasoning effort are
changed with `/model` and `/effort` instead, which apply to the live session
without a restart and are saved as the primary's defaults for future sessions.

<figure class="shot">
  <a class="shot__zoom" href={shotSessionConfig.src} aria-label="Open the full-size screenshot: the configuration editor">
    <Image src={shotSessionConfig} alt="The Mjolnir Web configuration editor showing Team, Reviewer, Subagents, ACP servers, Input, and Appearance sections, with Codex session options for mode, collaboration mode, reasoning effort, and fast mode." />
  </a>
  <figcaption>The configuration editor in the browser, with the connected adapter’s own session options underneath Mjolnir’s.</figcaption>
</figure>

## Reach it from another device

### Tailscale

```bash
mj server
```

`mj server` uses Tailscale when it finds a running tailnet node with MagicDNS and
HTTPS Certificates enabled. Mjolnir then binds to network interfaces, asks
`tailscale cert` for the machine's `ts.net` certificate, and renews it. If it
cannot get a Tailscale certificate, it falls back to loopback. Tailnet
reachability and ACLs are part of the security boundary.

```bash
mj server --no-tailscale-detect
```

`--no-tailscale-detect` keeps the server loopback-only on a tailnet machine.
`--hostname` also skips detection because that hostname is the one the login QR
must point at. The old `--tailscale` flag is accepted as a deprecated no-op.

### Public hostname

```bash
mj server --hostname mj.example.com
```

This binds to network interfaces and generates a self-signed certificate for the
supplied hostname. It does not provision DNS, a trusted public certificate, a
reverse proxy, firewall rules, or internet authentication. Do not expose this
mode directly to an untrusted network without designing those layers.

## Retention and sign-out

```bash
mj server \
  --history-days 7 \
  --session-ttl-days 2
```

- `--history-days 0` keeps disconnected session history indefinitely.
- `--session-ttl-days 0` makes viewer sessions ephemeral.
- `--logout-all` rotates the cookie signing key and signs every viewer out. The
  underlying QR/bearer login token remains available for reauthentication.

Remote state includes local SQLite session/transcript data, queued prompts,
permission decisions, authentication material, and certificates under Mjolnir's
platform state/config directories.

## Before leaving loopback

1. Decide who can reach the server's port (11921 unless `--port` changed it) and
   enforce that with host or tailnet policy.
2. Protect the login token, cookie key, certificates, and transcript storage.
3. Set finite history and session lifetimes.
4. Confirm remote users understand the active workspace and permission mode.
5. Test `--logout-all` and recovery before relying on remote access.
6. Keep the host patched; the server can drive provider agents and answer nested permissions.

See [Data and trust boundaries](/data-boundaries/) and [Storage and network
activity](/storage-network/) for the complete surface.
