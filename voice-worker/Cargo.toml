[package]
name = "belgr-mj-voice-worker"
version.workspace = true
edition = "2024"
rust-version = "1.97"
description = "Native local speech-recognition sidecar for belgr"
license = "GPL-3.0-only"
repository = "https://github.com/foundev/belgr"
publish = true

[[bin]]
name = "belgr-voice-worker"
path = "src/main.rs"

[dependencies]
anyhow = "1"
bzip2 = "0.6"
cpal = "0.16"
dirs = "6"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls", "stream"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sherpa-onnx = { version = "1.13.2", default-features = false, features = ["static"] }
tar = "0.4"

[dev-dependencies]
tempfile = "3"
